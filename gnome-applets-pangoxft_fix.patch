diff -aurN gnome-applets-2.11.91.orig/configure.in gnome-applets-2.11.91/configure.in
--- gnome-applets-2.11.91.orig/configure.in	2005-08-07 18:44:40.000000000 +0200
+++ gnome-applets-2.11.91/configure.in	2005-08-14 22:33:45.000000000 +0200
@@ -21,6 +21,7 @@
 
 dnl Stuff we might need, not quite sure yet
 
+PANGOXFT_REQUIRED=1.9.1
 GTK_REQUIRED=2.5.0
 GLIB_REQUIRED=2.5.0
 LIBGNOME_REQUIRED=2.8.0
@@ -84,6 +85,10 @@
 dnl ** pkg-config dependency checks         **
 dnl ******************************************
 
+PKG_CHECK_MODULES(PANGOXFT, pangoxft >= $PANGOXFT_REQUIRED)
+AC_SUBST(PANGOXFT_CFLAGS)
+AC_SUBST(PANGOXFT_LIBS)
+
 PKG_CHECK_MODULES(GAILUTIL, gail >= $LIBGAIL_REQUIRED)
 AC_SUBST(GAILUTIL_CFLAGS)
 AC_SUBST(GAILUTIL_LIBS)
diff -aurN gnome-applets-2.11.91.orig/gswitchit/Makefile.am gnome-applets-2.11.91/gswitchit/Makefile.am
--- gnome-applets-2.11.91.orig/gswitchit/Makefile.am	2005-07-28 15:12:00.000000000 +0200
+++ gnome-applets-2.11.91/gswitchit/Makefile.am	2005-08-14 23:03:49.000000000 +0200
@@ -33,13 +33,15 @@
 	      $(X_LIBS)
 
 libgswitchit_applet_a_CFLAGS = $(COMMON_CFLAGS) \
-           $(GNOME_APPLETS_CFLAGS)
+           $(GNOME_APPLETS_CFLAGS) \
+           $(PANGOXFT_CFLAGS)
 
 gnome_keyboard_applet_CFLAGS = $(COMMON_CFLAGS) \
            $(GNOME_APPLETS_CFLAGS)
 
 gnome_keyboard_applet_LDADD = $(COMMON_LIBS) \
                          $(GNOME_APPLETS_LIBS) \
+                         $(PANGOXFT_LIBS) \
                          $(top_builddir)/gswitchit/libgswitchit-applet.a \
                          $(top_builddir)/libgswitchit/libgswitchit.a  \
                          $(top_builddir)/libkbdraw/libkbdraw.a 
@@ -87,6 +89,7 @@
 gswitchit_plugins_capplet_CFLAGS = $(COMMON_CFLAGS) \
             $(GNOME_LIBS2_CFLAGS)
 
+
 gswitchit_plugins_capplet_LDFLAGS = -export-dynamic
 
 gswitchit_plugins_capplet_LDADD = $(COMMON_LIBS) \
